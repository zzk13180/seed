# =============================================================================
# 列出地图服务 (ListMaps.srv)
# =============================================================================
# 功能描述:
#   扫描指定目录, 列出所有可用的地图文件夹
#   只返回包含 map.yaml 文件的子目录名称
#
# 使用场景:
#   - 获取可用地图列表供用户选择
#   - 构建地图管理界面
#   - 验证地图是否存在
#
# 调用示例:
#   rosservice call /seed_core/list_maps "directory: '~/.seed/maps'"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string directory      # 要扫描的目录路径
                      # 例如: "~/.seed/maps"
                      # 支持 ~ 波浪号展开
---
# ========================= 响应参数 (Response) ================================
string[] maps         # 地图名称列表 (按字母顺序排序)
                      # 每个元素为包含 map.yaml 的子目录名称
                      # 例如: ["default", "floor1", "floor2"]
