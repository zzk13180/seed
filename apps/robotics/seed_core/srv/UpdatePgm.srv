# =============================================================================
# 更新PGM地图文件服务 (UpdatePgm.srv)
# =============================================================================
# 功能描述:
#   将修改后的地图数据写入PGM文件
#   支持创建新地图或更新现有地图
#
# 使用场景:
#   - 保存地图编辑结果
#   - 地图修复和优化
#   - 虚拟墙/禁行区标注
#   - 地图降噪处理
#
# 调用示例:
#   rosservice call /seed_core/update_pgm "{file_path: '/path/to/map.pgm', 
#                                           width: 100, height: 100, data: [...]}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string file_path      # PGM文件保存路径
                      # 例如: "~/.seed/maps/default/map.pgm"
                      # 如果目录不存在会自动创建

int32 width           # 图像宽度 (像素)
                      # 必须大于0

int32 height          # 图像高度 (像素)
                      # 必须大于0

int32[] data          # 图像像素数据 (一维数组)
                      # 长度必须等于 width * height
                      # 值范围: 0-255
                      # 按行优先顺序存储
---
# ========================= 响应参数 (Response) ================================
bool success          # 操作是否成功
                      # true: 文件保存成功
                      # false: 保存失败

string message        # 返回消息
                      # 成功时: 包含保存的图像尺寸信息
                      # 失败时: 包含具体的错误原因
