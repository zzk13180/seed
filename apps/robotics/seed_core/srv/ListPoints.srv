# =============================================================================
# 列出导航点服务 (ListPoints.srv)
# =============================================================================
# 功能描述:
#   查询并返回符合条件的所有导航点位列表
#   支持按类型、地图、启用状态等条件过滤
#
# 使用场景:
#   - 获取地图上所有可用点位
#   - 构建点位选择列表
#   - 导出点位数据
#
# 调用示例:
#   # 获取所有导航点
#   rosservice call /seed_core/point_manager/list_points "{point_type: 'navigation', 
#                                                          map_name: '', 
#                                                          enabled_only: true}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string point_type            # 点位类型过滤 (可选)
                             # "navigation" - 只返回导航点
                             # 留空: 返回所有类型

string map_name              # 地图名称过滤 (Base64编码, 可选)
                             # 例如: "default"
                             # 留空: 返回所有地图的点位

bool enabled_only            # 是否只返回启用的点位
                             # true: 只返回 enabled=true 的点位
                             # false: 返回所有点位(包括禁用的)
---
# ========================= 响应参数 (Response) ================================
bool success                 # 操作是否成功
                             # true: 查询成功
                             # false: 查询失败

string message               # 返回消息
                             # 成功时: "Found X points"
                             # 失败时: 包含具体的错误原因

int32 count                  # 返回的点位数量

string points_json           # 点位列表 (JSON字符串)
                             # 格式: [{"id": 1, "name": "...", ...}, ...]
                             # 其中 name/map_name/description 为Base64编码
                             # 完整字段参见 GetPoint.srv 的响应部分
