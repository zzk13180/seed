# =============================================================================
# 录制 Rosbag 服务 (RecordRosbag.srv)
# =============================================================================
# 功能描述:
#   启动或停止 rosbag 录制，用于记录机器人运行数据
#
# 使用场景:
#   - 记录调试数据
#   - 保存测试场景
#   - 数据回放分析
#
# 调用示例:
#   # 开始录制
#   rosservice call /seed_core/record_rosbag "{topics: ['/tf', '/scan'], 
#                                              start: true, 
#                                              path: '~/rosbags/test'}"
#   # 停止录制
#   rosservice call /seed_core/record_rosbag "{start: false}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string[] topics             # 要录制的话题列表
                            # 例如: ["/tf", "/scan", "/odom"]
                            # 如果为空，录制所有话题

bool start                  # true: 开始录制, false: 停止录制

string path                 # 保存路径（目录），支持 ~ 展开
                            # 例如: "~/rosbags/test_session"

---

# ========================= 响应参数 (Response) ================================
bool success                # 操作是否成功
string message              # 操作结果消息
