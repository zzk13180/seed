# =============================================================================
# 读取PGM地图文件服务 (ReadPgm.srv)
# =============================================================================
# 功能描述:
#   读取PGM格式的栅格地图文件, 返回图像的元数据和像素数据
#   主要用于地图编辑、可视化和分析
#
# 使用场景:
#   - 在Web界面显示地图
#   - 地图编辑和标注功能
#   - 地图数据分析和处理
#
# 调用示例:
#   rosservice call /seed_core/read_pgm "file_path: '/path/to/map.pgm'"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string file_path      # PGM文件的完整路径
                      # 例如: "~/.seed/maps/default/map.pgm"
                      # 必须是有效的 .pgm 文件
---
# ========================= 响应参数 (Response) ================================
bool success          # 操作是否成功
                      # true: 文件读取成功
                      # false: 读取失败

string message        # 返回消息
                      # 成功时: 包含图像尺寸信息
                      # 失败时: 包含具体的错误原因

int32 width           # 图像宽度 (像素)
int32 height          # 图像高度 (像素)
int32 max_val         # 最大灰度值 (通常为255)

int32[] data          # 图像像素数据 (一维数组)
                      # 按行优先顺序存储
                      # 数组长度 = width * height
                      # 值范围: 0-255
                      # 0=黑色(障碍物), 255=白色(自由空间), 205=灰色(未知)
