# =============================================================================
# 更新JSON文件服务 (UpdateJson.srv)
# =============================================================================
# 功能描述:
#   创建或更新JSON格式的配置/数据文件
#   自动处理目录创建和备份
#
# 使用场景:
#   - 保存配置参数
#   - 存储路径点数据
#   - 更新任务配置
#   - 保存用户设置
#
# 调用示例:
#   rosservice call /seed_core/update_json "{file_path: '/path/to/config.json', 
#                                            json_content: '{\"key\": \"value\"}'}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string file_path      # JSON文件保存路径
                      # 例如: "~/.seed/config/settings.json"
                      # 如果目录不存在会自动创建
                      # 如果文件存在会自动创建备份

string json_content   # JSON内容字符串
                      # 必须是有效的JSON格式
                      # 支持嵌套对象和数组
---
# ========================= 响应参数 (Response) ================================
bool success          # 操作是否成功
                      # true: 文件保存成功
                      # false: 保存失败

string message        # 返回消息
                      # 成功时: 包含记录数量等信息
                      # 失败时: 包含具体的错误原因(如JSON解析错误)
