# =============================================================================
# 删除导航点服务 (DeletePoint.srv)
# =============================================================================
# 功能描述:
#   从点位数据库中删除指定的导航点位
#   支持通过ID或名称+类型两种方式定位点位
#
# 使用场景:
#   - 清理不再使用的点位
#   - 批量删除过期点位
#   - 地图更新后清理旧点位
#
# 调用示例:
#   # 通过ID删除
#   rosservice call /seed_core/point_manager/delete_point "{point_id: 1}"
#   # 通过名称删除
#   rosservice call /seed_core/point_manager/delete_point "{name: '工作站1', 
#                                                           point_type: 'navigation'}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
int32 point_id               # 点位ID (可选)
                             # 如果提供, 将优先使用ID删除
                             # 传入0或负数表示不使用此参数

string name                  # 点位名称 (可选)
                             # 与 point_type 配合使用
                             # 如果不使用ID删除, 则必须提供此参数

string point_type            # 点位类型 (可选)
                             # "navigation" - 导航目标点
                             # 与 name 配合使用
---
# ========================= 响应参数 (Response) ================================
bool success                 # 操作是否成功
                             # true: 点位删除成功
                             # false: 删除失败(点位不存在或参数错误)

string message               # 返回消息
                             # 成功时: 包含删除的点位信息
                             # 失败时: 包含具体的错误原因
