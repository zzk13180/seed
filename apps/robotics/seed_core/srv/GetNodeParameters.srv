# =============================================================================
# 获取节点参数服务 (GetNodeParameters.srv)
# =============================================================================
# 功能描述:
#   获取指定 ROS 节点的所有参数配置
#
# 使用场景:
#   - 调试节点配置
#   - 查看运行时参数
#   - 配置管理界面展示
#
# 调用示例:
#   rosservice call /seed_core/get_node_parameters "{node: '/move_base'}"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string node                 # 节点名称，例如 "/move_base"

---

# ========================= 响应参数 (Response) ================================
string parameters           # 参数信息（YAML 或 JSON 格式字符串）
