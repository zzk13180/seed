# =============================================================================
# 加载地图服务 (LoadMap.srv)
# =============================================================================
# 功能描述:
#   用于加载指定路径的地图文件到ROS导航系统中
#   该服务会重启导航模式并使用新的地图进行定位和路径规划
#
# 使用场景:
#   - 切换到不同的地图进行导航
#   - 系统启动时加载指定地图
#   - 多楼层或多区域导航时切换地图
#
# 调用示例:
#   rosservice call /seed_core/load_map "file_path: '/path/to/map'"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string file_path      # 地图文件路径 (YAML文件路径, 不含.yaml后缀)
                      # 例如: "~/.seed/maps/default/map"
                      # 系统会自动寻找对应的 .yaml 和 .pgm 文件
---
# ========================= 响应参数 (Response) ================================
bool success          # 操作是否成功
                      # true: 地图加载成功, 导航模式已启动
                      # false: 地图加载失败, 查看message获取错误信息
string message        # 返回消息
                      # 成功时: 包含加载的地图信息
                      # 失败时: 包含具体的错误原因
