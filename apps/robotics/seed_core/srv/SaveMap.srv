# =============================================================================
# 保存地图服务 (SaveMap.srv)
# =============================================================================
# 功能描述:
#   用于保存当前SLAM系统构建的地图到指定路径
#   保存后会生成 .yaml 元数据文件和 .pgm 栅格地图文件
#
# 使用场景:
#   - 建图完成后保存地图
#   - 更新现有地图
#   - 备份当前导航地图
#
# 调用示例:
#   rosservice call /seed_core/save_map "file_path: '/path/to/save' topic: '/map'"
# =============================================================================

# ========================= 请求参数 (Request) =================================
string file_path      # 保存路径 (不含文件扩展名)
                      # 例如: "~/.seed/maps/my_map"
                      # 系统会在该路径下生成 map.yaml 和 map.pgm 文件

string topic          # 地图话题名称
                      # 默认为 "/map"
                      # 可指定其他地图话题如 "/slam_toolbox/map"
---
# ========================= 响应参数 (Response) ================================
bool success          # 操作是否成功
                      # true: 地图保存成功
                      # false: 保存失败, 查看message获取错误信息

string message        # 返回消息
                      # 成功时: 包含保存的文件路径
                      # 失败时: 包含具体的错误原因
