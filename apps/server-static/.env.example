# Assets 服务器配置

# 服务器监听端口（默认：8080）
PORT=8080

# 服务器监听地址（默认：0.0.0.0）
HOST=0.0.0.0

# 静态文件缓存有效期（单位：毫秒，默认：3600000 = 1小时）
# 设置为 0 禁用缓存（仅用于开发调试）
CACHE_MAX_AGE=3600000

# 自定义静态文件目录路径
# 如不设置，自动根据运行环境选择 public 或 dist/public
# PUBLIC_DIR=/path/to/public

# 日志级别（默认：info）
# 可选值：trace, debug, info, warn, error, fatal, silent
LOG_LEVEL=info
