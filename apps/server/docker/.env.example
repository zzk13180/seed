# ========================================
# Server API Docker Compose 环境变量
# ========================================
# 复制此文件为 .env 并修改配置值
# cp .env.example .env

# JWT 配置 (生产环境必须修改)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-256-bits

# 数据库配置
DB_PASSWORD=your-db-password

# Redis 配置
REDIS_PASSWORD=your-redis-password

# 镜像标签 (生产环境使用)
IMAGE_TAG=latest
